<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
  <%= render @users %> <!-- Rails автоматически ищет партиал _user.html.erb -->
</ul>
<%= will_paginate %>
